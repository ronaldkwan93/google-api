
const Form = () => {
    const handleSubmit = (e) => {
        e.preventDefault();
    }
  return (
    <form>
      <input />
      <button onClick={handleSubmit}>Submit</button>
    </form>
  )
}

export default Form
